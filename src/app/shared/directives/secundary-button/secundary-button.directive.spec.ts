/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SecundaryButtonDirective } from './secundary-button.directive';

describe('Directive: SecundaryButton', () => {
  it('should create an instance', () => {
    const directive = new SecundaryButtonDirective();
    expect(directive).toBeTruthy();
  });
});
