/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { UpdateAdmobVideoRewardService } from './update-admob-video-reward.service';

describe('Service: UpdateAdmobVideoReward', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [UpdateAdmobVideoRewardService]
    });
  });

  it('should ...', inject([UpdateAdmobVideoRewardService], (service: UpdateAdmobVideoRewardService) => {
    expect(service).toBeTruthy();
  }));
});
