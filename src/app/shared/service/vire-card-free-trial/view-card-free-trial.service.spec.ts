/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { ViewCardFreeTrialService } from './view-card-free-trial.service';

describe('Service: ViewCardFreeTrial', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [ViewCardFreeTrialService]
    });
  });

  it('should ...', inject([ViewCardFreeTrialService], (service: ViewCardFreeTrialService) => {
    expect(service).toBeTruthy();
  }));
});
